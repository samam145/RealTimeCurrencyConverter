# RealTimeCurrencyConverter
CurrencyConverter is a Java-based CLI app for real-time currency conversion. It features live data fetching via the ExchangeRate-API, direct PKR conversion to major currencies (USD, EUR, AUD, AED, CAD), and a menu-driven interface. It also automatically maintains a local transaction history in a text file for easy tracking and record-keeping.
.
